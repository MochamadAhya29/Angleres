<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.mochamadahya.angleres">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Angleres.NoActionBar">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activity.maps.MapsKolam"
            android:label="@string/title_activity_maps_kolam"></activity>
        <activity
            android:name=".activity.maps.MapsLaut"
            android:label="@string/title_activity_maps_laut" />
        <activity
            android:name=".activity.maps.MapsDanau"
            android:label="@string/title_activity_maps_danau" />
        <activity
            android:name=".activity.maps.MapsSungai"
            android:label="@string/title_activity_maps_sungai" />
        <activity
            android:name=".activity.lokasi.KolamActivity"
            android:parentActivityName=".activity.LokasiActivity"
            android:theme="@style/Theme.Angleres" />
        <activity
            android:name=".activity.lokasi.LautActivity"
            android:parentActivityName=".activity.LokasiActivity"
            android:theme="@style/Theme.Angleres" />
        <activity
            android:name=".activity.lokasi.DanauActivity"
            android:parentActivityName=".activity.LokasiActivity"
            android:theme="@style/Theme.Angleres" />
        <activity
            android:name=".activity.lokasi.SungaiActivity"
            android:parentActivityName=".activity.LokasiActivity"
            android:theme="@style/Theme.Angleres" />
        <activity
            android:name=".activity.PrivasiActivity"
            android:parentActivityName=".activity.PengaturanActivity"
            android:theme="@style/Theme.Angleres" />
        <activity
            android:name=".activity.PengaturanActivity"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.Angleres" />
        <activity android:name=".activity.EditPostinganActivity" />
        <activity android:name=".activity.TokoActivity" />
        <activity
            android:name=".activity.ProfileActivity"
            android:parentActivityName=".activity.PengaturanActivity"
            android:theme="@style/Theme.Angleres" />
        <activity
            android:name=".activity.PenyewaanActivity"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.Angleres" />
        <activity
            android:name=".activity.LokasiActivity"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.Angleres" />
        <activity
            android:name=".activity.LiveStreamingActivity"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.Angleres" />
        <activity
            android:name=".activity.EventActivity"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.Angleres" />
        <activity
            android:name=".activity.DetailActivity"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.Angleres" />
        <activity
            android:name=".activity.CommentActivity"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.Angleres" />
        <activity
            android:name=".activity.BisnisActivity"
            android:theme="@style/Theme.Angleres" />
        <activity android:name=".activity.auth.UpdateEmailActivity" />
        <activity
            android:name=".activity.auth.ResetPasswordActivity"
            android:parentActivityName=".activity.auth.LoginActivity"
            android:theme="@style/Theme.Angleres" />
        <activity android:name=".activity.auth.ChangePasswordActivity" />
        <activity android:name=".activity.auth.RegisterActivity" />
        <activity android:name=".activity.auth.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.UploadVideoActivity"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.Angleres" />
        <activity android:name=".activity.AddVideoBeritaActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>